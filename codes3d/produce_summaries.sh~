#!/bin/bash


#repdir=/mnt/3dgenome/projects/tfad334/hvn/analysis/control/dbsnps1200/snps/1
repdir=repdir=/mnt/3dgenome/projects/tfad334/GWAS-Catalog-Analysis/results/undone

for i in $(ls $repdir);
do
    echo 'Producing summary for...'
    #echo -e '\t trait \t'$i
	#echo -e '\t replicate \t'$i
    #python produce_summary.py -e $repdir/$i/eqtls.txt -o $repdir/$i  -c ../docs/codes3d.conf
	if [-f "$repdir/$i/significant_etls.txt"] 
	then
		echo "Done!"
	fi
done
